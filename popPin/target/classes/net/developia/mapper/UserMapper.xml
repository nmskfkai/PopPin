<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.developia.mapper.UserMapper">

    <insert id="insertUser">
        INSERT INTO USERS (username, email, enabled, nickname, password, provider, provider_id)
        VALUES (#{username}, #{email}, #{enabled}, #{nickname}, #{password}, #{provider}, null)
    </insert>

    <select id="findByEmail" resultType="net.developia.domain.UserDTO">
        SELECT * FROM USERS WHERE email = #{email}
    </select>

    <select id="findByResetToken" resultType="net.developia.domain.UserDTO">
        SELECT * FROM USERS WHERE reset_token = #{token}
    </select>

    <update id="updateResetToken">
        UPDATE USERS SET reset_token = #{token} WHERE email = #{email}
    </update>

    <update id="updatePassword">
        UPDATE USERS SET password = #{newPassword} WHERE email = #{email}
    </update>

</mapper>
